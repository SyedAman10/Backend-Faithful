# 📬 Push Notifications Architecture & Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     FAITHFUL COMPANION APP                       │
│                                                                   │
│  ┌──────────────┐         ┌──────────────┐                      │
│  │ Expo SDK     │────────▶│ Push Token   │                      │
│  │ Notifications│         │ Registration │                      │
│  └──────────────┘         └──────────────┘                      │
│                                  │                               │
└──────────────────────────────────┼───────────────────────────────┘
                                   │
                                   │ PUT /api/users/preferences
                                   │ {pushToken, notificationSettings}
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                        BACKEND SERVER                            │
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐     │
│  │                 PostgreSQL Database                    │     │
│  │                                                        │     │
│  │  users table:                                         │     │
│  │  - push_token VARCHAR(255)                           │     │
│  │  - notification_settings JSONB                       │     │
│  │    {                                                  │     │
│  │      pushEnabled: true,                              │     │
│  │      journeyReminders: true,                         │     │
│  │      prayerUpdates: true                             │     │
│  │    }                                                  │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌─────────────────────────────────────────────────────┐        │
│  │           Notification Triggers                      │        │
│  │                                                      │        │
│  │  1. Prayer Response (POST /prayer/responses/reply)  │        │
│  │     └─▶ sendPrayerResponseNotification()           │        │
│  │                                                      │        │
│  │  2. Journey Reminder (Cron: Daily 9 AM)            │        │
│  │     └─▶ sendJourneyReminderNotification()          │        │
│  └─────────────────────────────────────────────────────┘        │
│                          │                                        │
└──────────────────────────┼────────────────────────────────────────┘
                           │
                           │ HTTPS POST to Expo Push API
                           │ https://exp.host/--/api/v2/push/send
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  EXPO PUSH NOTIFICATION SERVICE                  │
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │  1. Validate token                                 │         │
│  │  2. Queue notification                             │         │
│  │  3. Send to device (Apple APNS / Google FCM)      │         │
│  │  4. Return ticket ID                               │         │
│  └────────────────────────────────────────────────────┘         │
└───────────────────────────────────┬─────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        ┌──────────────────┐           ┌──────────────────┐
        │   iOS Device     │           │ Android Device   │
        │   (APNS)         │           │   (FCM)          │
        └──────────────────┘           └──────────────────┘
                    │                               │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                ┌─────────────────────────────────────┐
                │  Notification Displayed on Device   │
                │                                     │
                │  User Taps ───▶ Navigate to Screen │
                └─────────────────────────────────────┘
```

---

## Flow Diagrams

### 1. Prayer Response Notification Flow

```
┌──────────────┐
│ User B       │
│ Posts Reply  │
└──────┬───────┘
       │
       │ POST /api/prayer/responses/:responseId/reply
       ▼
┌─────────────────────────────────────┐
│ Backend: Prayer Route Handler       │
│                                     │
│ 1. Validate reply                   │
│ 2. Save to database                 │
│ 3. Get prayer request author        │
└──────┬──────────────────────────────┘
       │
       │ Query: Get author's push_token & settings
       ▼
┌─────────────────────────────────────┐
│ Check Notification Eligibility      │
│                                     │
│ ✓ Has push_token?                   │
│ ✓ pushEnabled = true?               │
│ ✓ prayerUpdates = true?             │
│ ✓ Not self-response?                │
└──────┬──────────────────────────────┘
       │
       │ All checks pass
       ▼
┌─────────────────────────────────────┐
│ sendPrayerResponseNotification()    │
│                                     │
│ Payload:                            │
│ {                                   │
│   title: "🙏 Someone responded"    │
│   body: "John Doe responded..."    │
│   data: {                           │
│     type: "prayer_response",       │
│     requestId: 456,                │
│     responseId: 789                │
│   }                                 │
│ }                                   │
└──────┬──────────────────────────────┘
       │
       │ POST https://exp.host/--/api/v2/push/send
       ▼
┌─────────────────────────────────────┐
│ Expo Push Service                   │
│                                     │
│ Returns ticket ID                   │
└──────┬──────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────┐
│ User A's Device                     │
│                                     │
│ 🔔 Notification appears             │
│                                     │
│ User taps ───▶ PrayerDetail screen  │
└─────────────────────────────────────┘
```

### 2. Journey Reminder Flow

```
┌─────────────────────┐
│ Cron Job            │
│ Runs: Daily 9:00 AM │
└──────┬──────────────┘
       │
       │ utils/journeyReminderCron.js
       ▼
┌──────────────────────────────────────┐
│ Query: Find Eligible Users           │
│                                      │
│ SELECT users WHERE:                  │
│  - Completed journey day 20-30h ago  │
│  - push_token IS NOT NULL            │
│  - pushEnabled = true                │
│  - journeyReminders = true           │
│  - Next day not completed            │
└──────┬───────────────────────────────┘
       │
       │ Results: Array of users
       ▼
┌──────────────────────────────────────┐
│ Loop Through Users                   │
│                                      │
│ For each user:                       │
│   1. Get next_day number             │
│   2. Call sendJourneyReminder()      │
│   3. Wait 100ms (rate limiting)      │
└──────┬───────────────────────────────┘
       │
       │ For each user
       ▼
┌──────────────────────────────────────┐
│ sendJourneyReminderNotification()    │
│                                      │
│ Payload:                             │
│ {                                    │
│   title: "📖 Your Journey Awaits"   │
│   body: "Day 5 is ready for you..." │
│   data: {                            │
│     type: "journey_reminder",       │
│     day: 5                           │
│   }                                  │
│ }                                    │
└──────┬───────────────────────────────┘
       │
       │ POST https://exp.host/--/api/v2/push/send
       ▼
┌──────────────────────────────────────┐
│ Expo Push Service                    │
│                                      │
│ Batch sends to all users             │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Multiple Users' Devices              │
│                                      │
│ 🔔 Notifications appear              │
│                                      │
│ Users tap ───▶ Journey Day 5 screen  │
└──────────────────────────────────────┘
```

### 3. Push Token Registration Flow

```
┌──────────────────────┐
│ Mobile App           │
│ First Launch / Login │
└──────┬───────────────┘
       │
       │ Request notification permissions
       ▼
┌──────────────────────────────────────┐
│ Expo Notifications.requestPermissions │
└──────┬───────────────────────────────┘
       │
       │ User grants permission
       ▼
┌──────────────────────────────────────┐
│ Expo.getExpoPushTokenAsync()         │
│                                      │
│ Returns:                             │
│ "ExponentPushToken[xxxxxxxxxx...]"   │
└──────┬───────────────────────────────┘
       │
       │ PUT /api/users/preferences
       │ Body: { pushToken: "...", notificationSettings: {...} }
       ▼
┌──────────────────────────────────────┐
│ Backend: routes/users.js             │
│                                      │
│ 1. Validate token format             │
│ 2. Validate settings structure       │
│ 3. Update users table                │
│ 4. Return success                    │
└──────┬───────────────────────────────┘
       │
       │ UPDATE users SET push_token = $1, notification_settings = $2
       ▼
┌──────────────────────────────────────┐
│ PostgreSQL Database                  │
│                                      │
│ User record updated:                 │
│ ✓ push_token saved                   │
│ ✓ notification_settings saved        │
└──────────────────────────────────────┘
```

---

## Component Interactions

### Backend Components

```
┌────────────────────────────────────────────────────────┐
│                     server.js                           │
│                                                         │
│  • Initializes Express app                             │
│  • Starts cron job: startJourneyReminderCron()         │
│  • Registers routes                                    │
└────────────┬───────────────────────────────────────────┘
             │
             ├──▶ routes/users.js
             │    • PUT /preferences (save push token)
             │
             ├──▶ routes/prayer.js
             │    • POST /responses/:id/reply (trigger notification)
             │
             └──▶ utils/journeyReminderCron.js
                  • Cron job runs daily at 9 AM
                  • Queries eligible users
                  • Sends journey reminders
                       │
                       └──▶ utils/pushNotifications.js
                            • sendPushNotification()
                            • sendPrayerResponseNotification()
                            • sendJourneyReminderNotification()
                            • sendCustomNotification()
                                 │
                                 └──▶ Expo Push API
                                      https://exp.host/--/api/v2/push/send
```

### Data Flow

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Mobile    │──────▶│   Backend   │──────▶│  Database   │
│    App      │ Token │   Server    │ Store │  (Postgres) │
└─────────────┘       └─────────────┘       └─────────────┘
      │                      │                      │
      │                      │ Query users          │
      │                      │◀─────────────────────┘
      │                      │
      │                      │ Send notification
      │                      └──────────────────────┐
      │                                             │
      │                      ┌─────────────┐        │
      │                      │ Expo Push   │◀───────┘
      │                      │  Service    │
      │                      └─────────────┘
      │                             │
      │◀────────────────────────────┘
      │   Notification delivered
      │
┌─────┴─────────┐
│ User receives │
│ notification  │
└───────────────┘
```

---

## Database Schema Relationships

```
┌──────────────────────────────────────────────────┐
│                  users table                      │
├──────────────────────────────────────────────────┤
│ id (primary key)                                 │
│ email                                            │
│ name                                             │
│ push_token VARCHAR(255) ◀────────────────────────┼─── Expo Push Token
│ notification_settings JSONB {                    │    "ExponentPushToken[...]"
│   "pushEnabled": true,                          │
│   "journeyReminders": true,  ◀──────────────────┼─── Controls Journey Reminders
│   "prayerUpdates": true      ◀──────────────────┼─── Controls Prayer Notifications
│ }                                                │
│ ...other fields                                  │
└──────────────┬───────────────────────────────────┘
               │
               │ References
               │
    ┌──────────┴─────────────┐
    │                        │
    ▼                        ▼
┌─────────────────┐  ┌──────────────────────┐
│ prayer_requests │  │ user_journey_progress│
│                 │  │                      │
│ user_id (FK)    │  │ user_id (FK)         │
│ ...             │  │ day                  │
└─────────────────┘  │ completed            │
                     │ completed_at         │
                     └──────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────┐
│ Send Notification   │
└──────┬──────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Validate Push Token                  │
│                                      │
│ Invalid? ──▶ Return error, log       │
│             Don't fail request       │
└──────┬───────────────────────────────┘
       │ Valid
       ▼
┌──────────────────────────────────────┐
│ POST to Expo Push API                │
└──────┬───────────────────────────────┘
       │
       ├──▶ Success
       │    └─▶ Log ticket ID
       │        Return success
       │
       └──▶ Error
            ├─▶ Token invalid/expired
            │   └─▶ Log warning
            │       (User needs to refresh token)
            │
            ├─▶ Rate limit
            │   └─▶ Log error
            │       Retry later (for cron)
            │
            └─▶ Network error
                └─▶ Log error
                    Fire-and-forget for prayer responses
                    Retry for cron (next iteration)
```

---

## Security & Privacy Flow

```
┌─────────────────────┐
│ User makes request  │
└──────┬──────────────┘
       │
       │ JWT Token in Authorization header
       ▼
┌─────────────────────────────────────┐
│ authenticateToken middleware        │
│                                     │
│ Validates JWT                       │
│ Extracts user ID                    │
└──────┬──────────────────────────────┘
       │ Authorized
       ▼
┌─────────────────────────────────────┐
│ Route Handler                       │
│                                     │
│ • Can only update own push token    │
│ • Can only update own settings      │
│ • Respects anonymous prayer flags   │
└──────┬──────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────┐
│ Notification Trigger                │
│                                     │
│ • Checks user preferences           │
│ • Respects opt-outs                 │
│ • No self-notifications             │
│ • Anonymous mode respected          │
└─────────────────────────────────────┘
```

---

## Monitoring & Logging Flow

```
┌─────────────────────────────────────────────────┐
│              Application Events                  │
└──────┬──────────────────────────────────────────┘
       │
       ├──▶ Push Token Update
       │    Console: 📱 Push Token update: {...}
       │
       ├──▶ Notification Settings Update
       │    Console: 🔔 Notification Settings update: {...}
       │
       ├──▶ Prayer Notification Sent
       │    Console: 📤 Sending prayer response notification: {...}
       │    Console: ✅ Push notification sent successfully: ticket-id
       │
       ├──▶ Journey Reminder Cron Start
       │    Console: 🔔 ======== Journey Reminder Cron Job Started ========
       │    Console: 📊 Found X users eligible for journey reminders
       │
       ├──▶ Journey Reminder Sent
       │    Console: 📤 Sending reminder to user X for day Y
       │    Console: ✅ Reminder sent to user X
       │
       ├──▶ Cron Job Complete
       │    Console: 📊 Journey Reminder Summary: {...}
       │    Console: ======== Journey Reminder Cron Job Completed ========
       │
       └──▶ Errors
            Console: ❌ Failed to send push notification: {...}
            Console: ❌ Error sending notification: {...}
```

---

## Performance Considerations

```
┌────────────────────────────────────────────────────┐
│              Optimization Strategies                │
└────────────────────────────────────────────────────┘

1. Database Indexing
   ┌─────────────────────────────────────┐
   │ idx_users_push_token                │
   │ • Fast lookup of users with tokens  │
   │ • WHERE push_token IS NOT NULL      │
   └─────────────────────────────────────┘

2. Fire-and-Forget for Prayer Responses
   ┌─────────────────────────────────────┐
   │ sendPrayerResponseNotification()    │
   │ • Async, non-blocking               │
   │ • Errors logged, don't fail request │
   │ • User experience not affected      │
   └─────────────────────────────────────┘

3. Rate Limiting in Cron
   ┌─────────────────────────────────────┐
   │ Journey Reminder Cron               │
   │ • 100ms delay between sends         │
   │ • Prevents Expo rate limits         │
   │ • Batch processing for scale        │
   └─────────────────────────────────────┘

4. JSONB for Settings
   ┌─────────────────────────────────────┐
   │ notification_settings JSONB         │
   │ • Flexible schema                   │
   │ • Fast JSON queries in PostgreSQL   │
   │ • Easy to add new settings          │
   └─────────────────────────────────────┘

5. Single Query for Eligibility
   ┌─────────────────────────────────────┐
   │ Cron Job Query                      │
   │ • Joins + filters in one query      │
   │ • Reduces database round trips      │
   │ • Efficient user selection          │
   └─────────────────────────────────────┘
```

---

## Scalability

```
Current Implementation:
┌────────────────────────────────────┐
│ Single Server                      │
│ • Handles all notifications        │
│ • Cron runs on server instance     │
│ • Good for 1-10K users             │
└────────────────────────────────────┘

Future Scaling Options:
┌────────────────────────────────────┐
│ 1. Message Queue (e.g., Redis)    │
│    • Decouple notification sending │
│    • Worker processes              │
│                                    │
│ 2. Dedicated Notification Service  │
│    • Microservice architecture     │
│    • Separate scaling              │
│                                    │
│ 3. Batch Processing                │
│    • Process in chunks             │
│    • Parallel workers              │
│                                    │
│ 4. Database Replication            │
│    • Read replicas for queries     │
│    • Write master for updates      │
└────────────────────────────────────┘
```

---

**Document Version:** 1.0.0  
**Last Updated:** December 1, 2025  
**Status:** ✅ Production Ready

